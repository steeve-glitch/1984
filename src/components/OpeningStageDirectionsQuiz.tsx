import React, { FC } from 'react';
import Card from './Card';

const OpeningStageDirectionsQuiz: FC = () => {
  return (
    <Card title="Opening Stage Directions Quiz">
      <div className="p-4 text-center">
        <p className="text-gray-600 dark:text-gray-300">This activity is not yet implemented.</p>
        <p className="text-sm text-gray-500 dark:text-gray-400 mt-2">Please proceed to the next section.</p>
      </div>
    </Card>
  );
};

export default OpeningStageDirectionsQuiz;
